# 📚 Концепція курсу: Web-програмування (Places Platform)
**Цільова аудиторія:** Студенти 3 курсу  
**Формат:** 6 бекенд-лабораторних робіт + 2 фронтенд-лабораторних роботи. Наскрізний проєкт.

## 🌟 1. Ключова ідея курсу
Всі лабораторні роботи є етапами розробки єдиного програмного продукту — повноцінної веб-платформи Places. Бекенд-лаби будують REST API; фронтенд-лаби будують веб-інтерфейс, що споживає цей API. Кожна наступна лабораторна робота логічно розширює функціональність попередньої.

Після завершення курсу студенти повинні вміти:

**Бекенд:**
- Проєктувати та розробляти REST API.
- Будувати правильну архітектуру backend-додатку (Layered architecture: controllers, services, repositories).
- Інтегрувати базу даних (PostgreSQL).
- Реалізовувати безпеку (Аутентифікація/Авторизація через JWT).
- Обробляти помилки та готувати додаток до деплою.

**Фронтенд:**
- Створювати SPA з компонентним підходом (React або Vue).
- Інтегрувати фронтенд з REST API через `fetch`/`axios`.
- Реалізовувати форми, CRUD-операції та клієнтську маршрутизацію.
- Працювати з JWT-аутентифікацією на стороні клієнта.

---

## 🎯 1.1. Опис наскрізного проєкту (Places API)
Протягом курсу студенти створюють єдиний продукт — **платформу для обміну цікавими локаціями (Places/Locations API)**. Це бекенд для додатку, що нагадує спрощений TripAdvisor або Foursquare.

**Основні сценарії використання (Business Logic):**
- **Перегляд контенту:** Будь-який користувач (навіть гість) може переглядати каталог місць, читати детальний опис, бачити адресу та читати відгуки інших людей.
- **Управління місцями:** Після реєстрації та авторизації користувач може створювати нові локації на платформі (ділитися своїми знахідками), а також редагувати чи видаляти **свої** місця.
- **Соціальна взаємодія:** Авторизовані користувачі можуть залишати коментарі та ставити оцінки (1-5 зірок) закладам чи локаціям.
- **Модерація:** Система підтримує рольову модель. Користувачі з роллю `admin` мають права на управління всім контентом (видалення будь-яких місць або відгуків у разі порушення правил).
- **Пошук:** Реалізована можливість фільтрації місць (наприклад, за містом) та пагінація для зручного перегляду великих списків.

---

## 🛠 2. Технологічний стек

### 2.1 Бекенд

Бекенд-лаби доступні у **трьох варіантах реалізації** — оберіть один стек на початку курсу та дотримуйтесь його протягом усіх 6 бекенд-лаб:

| Стек | Мова | Фреймворк | ORM | Валідація |
|------|------|-----------|-----|-----------|
| **Node.js** | JavaScript | Express | Sequelize / Prisma | Joi / Zod |
| **Python** | Python 3.11+ | FastAPI | SQLAlchemy (async) | Pydantic (вбудовано) |
| **.NET** | C# 12 | ASP.NET Core 8 | Entity Framework Core | Data Annotations |

Всі три варіанти реалізують **один і той самий** REST API для платформи Places та підключаються до однієї бази даних PostgreSQL. Концепції (REST, JWT, RBAC, пагінація) — спільні для всіх стеків.

- **База даних:** PostgreSQL.
- **Безпека:** JWT (JSON Web Tokens).
- **Інструменти:** Git, Postman, VS Code (або будь-який редактор коду).

### 2.2 Фронтенд

Фронтенд-лаби доступні у **двох варіантах реалізації** — оберіть один стек та дотримуйтесь його протягом усіх фронтенд-лаб:

| Стек | Мова | Фреймворк | Маршрутизація | Збірка |
|------|------|-----------|---------------|--------|
| **React** | JavaScript / TypeScript | React 18 | React Router | Vite |
| **Vue** | JavaScript / TypeScript | Vue 3 | Vue Router | Vite |

Обидва варіанти підключаються до **одного й того самого** бекенд API, який студент розробив у бекенд-лабах. HTTP-запити виконуються через `fetch` або `axios`.

---

## 🧩 3. Доменна модель (Базова схема БД)
- **User:** `id`, `email`, `password` (hashed), `role` (user/admin)
- **Place:** `id`, `name`, `description`, `city`, `address`, `createdBy` (User ID)
- **Review:** `id`, `rating` (1-5), `comment`, `userId` (User ID), `placeId` (Place ID)

---

## 📅 4. План лабораторних робіт

### Бекенд-лаби (6)

### 🔹 ЛР 1: Основи backend + Places API (In-memory)
**Фокус:** Node.js, Express, базові REST API принципи.  
**Результат:** Реалізовано базовий CRUD для сутності `Place` (збереження в пам'яті). Структуровано проєкт.

### 🔹 ЛР 2: Інтеграція Бази Даних
**Фокус:** Підключення ORM/ODM, асинхронна робота з БД, глобальна обробка помилок, базова валідація даних (напр. Joi / Zod).  
**Результат:** Дані сутності `Place` зберігаються в БД. Реалізовано централізований Error Handler. Вхідні дані валідуються перед збереженням.

### 🔹 ЛР 3: Робота з пов'язаними даними (Reviews)
**Фокус:** Реляції між таблицями/колекціями, вкладені маршрути.  
**Результат:** Додано сутність `Review`. Можливість залишати відгуки до конкретних місць та виводити їх списком.

### 🔹 ЛР 4: Аутентифікація та JWT
**Фокус:** Реєстрація, логін, хешування паролів, захист API.  
**Результат:** Реалізовано сутність `User`. Маршрути створення/редагування місць та відгуків захищені токеном.

### 🔹 ЛР 5: Просунуті запити (Фільтрація, Пагінація, Ролі)
**Фокус:** Обробка Query Params, RBAC (ролі).  
**Результат:** Пошук місць за містом/рейтингом, пагінація сторінок. Додано роль `admin`.

### 🔹 ЛР 6: Production Readiness
**Фокус:** Логування (Winston/Morgan), безпека (Helmet, CORS, Rate Limiting), підготовка до деплою.  
**Результат:** Додаток генерує логи, захищений від базових атак та готовий до розгортання.

---

### Фронтенд-лаби (2)

### 🔸 ФР 1: Налаштування проєкту та список Places
**Фокус:** Ініціалізація SPA (Vite + React або Vue), компонентна архітектура, HTTP-запит до API.  
**Результат:** Фронтенд-проєкт налаштований, реалізований компонент для відображення списку місць через `GET /api/places`.

### 🔸 ФР 2: Форми, CRUD та навігація
**Фокус:** Контрольовані форми, операції `POST`/`PUT`/`DELETE`, клієнтська маршрутизація.  
**Результат:** Реалізовані форми створення та редагування місць, навігація між сторінками додатку.

[//]: # (### ⭐ Додаткова ЛР &#40;Бонусна&#41;)
[//]: # (**Фокус:** Розширений функціонал &#40;Advanced Features&#41;.  )
[//]: # (**Результат:** "Улюблених місць" &#40;Favorites&#41;, топ місця.)

[//]: # (---)

[//]: # (## 📋 5. Стандарт структури лабораторної роботи)
[//]: # (Для подальшої промпт-генерації ЛР використовувати цей шаблон:)
[//]: # (1. **Мета роботи:** &#40;Що студент 3 курсу вивчає&#41;.)
[//]: # (2. **Теоретичні відомості:** &#40;Концентровано, тільки необхідне&#41;.)
[//]: # (3. **Завдання:** &#40;Чіткий покроковий список&#41;.)
[//]: # (4. **Вимоги до реалізації:** &#40;Моделі, API endpoints, файли&#41;.)
[//]: # (5. **Очікуваний результат:** &#40;Критерії приймання&#41;.)
[//]: # (6. **Контрольні питання:** &#40;5–10 шт&#41;.)

---

## 🔍 5. Загальні правила для студентів
- **Контроль версій:** Обов'язкове використання Git. Кожна лаба = окремий commit / branch.
- **Тестування бекенду:** Всі API тестуються через Postman.
- **Тестування фронтенду:** Перевірка функціоналу у браузері (Chrome DevTools → Network для відстеження запитів).
- **Структура бекенду:** Код структурований (Layered architecture), заборонено змішувати всі шари в одному файлі.
- **Структура фронтенду:** Компонентний підхід — кожен компонент відповідає за одну частину UI.
